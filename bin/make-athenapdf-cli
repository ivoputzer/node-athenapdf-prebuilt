#!/usr/bin/env sh

set -e

git config -f .gitmodules --get-regex submodule.*url \
| sed 's/submodule.//g;s/.url//g' \
| awk '{print $2,$1}' \
| xargs -n 2 git clone --recursive 2> /dev/null \
| sh

npm install lib/athenapdf/cli --save # prevents from install being re-run
